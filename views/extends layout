extends layout

block header

block content
 
  h3 Start a Build Process
  p
    | This process provides the ability to start and build a process. This process
    | will assign the next available port. It will handle the git checkout, build, 
    | npm install and deployment.
    
  

